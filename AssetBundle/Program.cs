using System;
using System.IO;

namespace GFTextMerge.AssetBundles
{
    internal class FakeProgram
    {
        private static void FakeMain(string[] args)
        {
            if (args.Length <= 0) return;
            
        }
    }
}
